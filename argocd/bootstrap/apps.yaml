apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet

metadata:
  name: bootstrap-apps
  namespace: argocd
  labels:
    gitops.trustin.tech/platform: "true"

spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/AnasSahel/gitops-crossplane-argocd.git
        revision: HEAD
        directories:
          - path: apps/*
  template:
    metadata:
      name: "app-{{.path.basename}}"
      labels:
        gitops.trustin.tech/app: "true"
    spec:
      project: default
      source:
        repoURL: https://github.com/AnasSahel/gitops-crossplane-argocd.git
        targetRevision: HEAD
        path: "{{.path.path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "app-{{.path.basename}}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
